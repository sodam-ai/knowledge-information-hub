# Knowledge Link Hub — 작업 체크포인트

> 마지막 업데이트: 2026-04-29  
> 브랜치: master  
> 저장소: https://github.com/sodam-ai/knowledge-link-hub  
> 로컬 경로: D:\AI_Dev_Work\2026y\26y_04m_12d_knowledge-link-hub  
> 포트: 8002 (`.env.local` `NEXT_PUBLIC_SITE_URL` 기준)

---

## 현재 상태 요약

사이트 단일 열람 비밀번호 인증 + 대시보드 전면 개선 + OG 썸네일 E2E 수정 완료.

- 빌드 정상 (`npm run build` 오류 없음)
- OG 썸네일 저장 검증 완료 (tailwindcss.com 등 실제 저장·DB 확인)
- 온보딩 페이지(`/onboarding`) 신규 구현
- 날짜별 피드 그룹 헤더, 핀 섹션 구현
- GitHub push 완료

---

## 아키텍처 개요

```
[브라우저] → /login → 열람 비밀번호(4자리) 입력
                    ↓ site-auth.ts::signIn()
            Supabase site_config 테이블에서 view_password_hash 조회
            (없으면 .env VIEW_PASSWORD 해시로 fallback)
                    ↓ 일치 시
            klh_session 쿠키 발급 (HMAC-SHA256, 7일)
                    ↓
[미들웨어] src/middleware.ts → 모든 페이지에서 쿠키 검증
           공개 경로: /login, /admin, /share, /api/og

[OG 썸네일] 클라이언트 SaveItemButton이 /api/og로 미리 가져온
            og:image URL을 hidden input "og_image"로 서버 액션에 전달.
            서버는 sanitizeImageUrl() 검증 후 thumbnail_url로 저장.
            (Microlink는 fallback, Windows에서 도달 불가 이슈 있음)
```

---

## 2026-04-29 세션 작업 내역

### 1. OG 썸네일 E2E 수정 ✅
| 파일 | 변경 내용 |
|------|-----------|
| `src/actions/items.ts` | `og_image` hidden input 수신 → `sanitizeImageUrl()` 검증 → `thumbnail_url` 저장 |
| `src/components/items/SaveItemButton.tsx` | `<input type="hidden" name="og_image" value={ogMeta?.image ?? ""} />` 추가 |

**검증**: `https://tailwindcss.com` 저장 → DB `thumbnail_url` 값 확인 ✅ → 카드 `<img>` 렌더 ✅

### 2. ItemFeed stale 상태 수정 ✅
| 파일 | 변경 내용 |
|------|-----------|
| `src/app/(dashboard)/dashboard/page.tsx` | `<ItemFeed key={...totalCount} ...>` — totalCount 변경 시 강제 재마운트 |

### 3. 온보딩 페이지 신규 구현 ✅
| 파일 | 변경 내용 |
|------|-----------|
| `src/app/onboarding/page.tsx` | 신규 생성 — 그룹명/설명/카테고리 폼, 생성 후 `/dashboard?team=ID` 이동 |
| `src/actions/teams.ts` | `createTeamNoAuth()` 추가 (서비스 클라이언트, 샘플 아이템 3개) |
| `src/components/layout/TeamHeader.tsx` | Plus 아이콘 → `/onboarding` 링크 |
| `src/app/(dashboard)/dashboard/page.tsx` | 빈 상태 UI — "새 그룹 만들기" + "관리자 설정" 버튼 |

### 4. 피드 UX 개선 ✅
| 파일 | 변경 내용 |
|------|-----------|
| `src/components/items/ItemFeed.tsx` | `groupItemsByDate()` — 오늘/어제/이번 주/이번 달/그 이전 헤더, 핀 섹션 분리 |
| `src/components/items/ItemCard.tsx` | 썸네일 항상 표시(`hidden sm:block` 제거), 링크 content 설명 줄 추가 |

---

## 환경 변수 (.env.local — git 제외됨)

| 변수명 | 역할 | 비고 |
|--------|------|------|
| `NEXT_PUBLIC_SUPABASE_URL` | Supabase 프로젝트 URL | 필수 |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | Supabase anon 키 | 필수 |
| `SUPABASE_SERVICE_ROLE_KEY` | 서버 전용 서비스 롤 키 | 필수, 절대 노출 금지 |
| `NEXT_PUBLIC_SITE_URL` | 사이트 URL (현재: `http://localhost:8002`) | 필수 |
| `VIEW_PASSWORD` | 초기 열람 비밀번호 (숫자 4자리) | 현재: `0000` — 배포 전 변경 |
| `ADMIN_PASSWORD` | 초기 관리자 비밀번호 | 현재: `0000` — 배포 전 변경 |
| `SESSION_SECRET` | 세션 서명 HMAC 시크릿 (64자 hex) | 절대 노출 금지 |
| `UPSTASH_REDIS_REST_URL` | Upstash Redis URL | Rate Limiting용 |
| `UPSTASH_REDIS_REST_TOKEN` | Upstash Redis 토큰 | Rate Limiting용 |
| `MICROLINK_API_KEY` | URL 제목 자동 추출 | 선택, Windows에서 미도달 이슈 |

---

## Supabase 마이그레이션 상태

| 파일 | 상태 |
|------|------|
| 001~008 | ✅ 실행 완료 |
| **009_site_config.sql** | ⚠️ **Supabase에 아직 미적용 시 반드시 실행** |

009번 실행 전까지는 `/admin` 비밀번호 변경 기능이 동작하지 않습니다.

---

## 보안 점검 결과

- `.env.local` — `.gitignore` 제외 ✅
- `client_secret_*.json` — `.gitignore` 패턴으로 제외 ✅
- 소스 코드 내 하드코딩 시크릿 없음 ✅
- `SUPABASE_SERVICE_ROLE_KEY` 서버 전용 파일에서만 사용 ✅

---

## 알려진 잔여 이슈

| 우선순위 | 이슈 | 비고 |
|----------|------|------|
| 낮음 | Microlink 서버사이드 도달 불가 (Windows Node.js) | 클라이언트 OG로 우회 완료, 실제 영향 없음 |
| 낮음 | ItemFeed key 재마운트 시 스크롤 위치 리셋 | UX 개선 여지 있음 |

---

## 다음 세션 시작 시

```bash
cd "D:\AI_Dev_Work\2026y\26y_04m_12d_knowledge-link-hub"
git log --oneline -3   # 커밋 상태 확인
git status             # 미커밋 파일 확인
npm run dev            # 포트 8002
```

접속: `http://localhost:8002` → 비밀번호: `.env.local`의 `VIEW_PASSWORD`

---

## 세션 재개용 지시문

```
D:\AI_Dev_Work\2026y\26y_04m_12d_knowledge-link-hub 프로젝트 이어서 작업.
.resume.md 참고 — OG 썸네일 수정·온보딩·피드 개선 완료, GitHub push 완료.
009_site_config.sql Supabase 적용 여부 먼저 확인 후 작업 시작.
```
