# Knowledge Link Hub — 작업 체크포인트

> 마지막 업데이트: 2026-04-28  
> 브랜치: master  
> 저장소: https://github.com/sodam-ai/knowledge-link-hub

---

## 현재 상태 요약

Supabase Auth 기반 멀티유저 인증 시스템을 **사이트 전체 단일 열람 비밀번호** 방식으로 전면 교체 완료.

- 회원가입·로그인·OAuth·PIN 인증 전부 제거
- 숫자 4자리 열람 비밀번호 + Web Crypto API HMAC-SHA256 세션 쿠키 방식으로 대체
- `/admin` 페이지에서 열람·관리자 비밀번호 변경 가능
- 빌드 정상 (`npm run build` 오류 없음, 10개 라우트 생성 확인)

---

## 아키텍처 개요

```
[브라우저] → /login → 열람 비밀번호(4자리) 입력
                    ↓ site-auth.ts::signIn()
            Supabase site_config 테이블에서 view_password_hash 조회
            (없으면 .env VIEW_PASSWORD 해시로 fallback)
                    ↓ 일치 시
            klh_session 쿠키 발급 (HMAC-SHA256, 7일)
                    ↓
[미들웨어] src/middleware.ts → 모든 페이지에서 쿠키 검증
           공개 경로: /login, /admin, /share, /api/og

[/admin 페이지] → 열람 비밀번호 변경 (관리자 비밀번호 확인 필요)
               → 관리자 비밀번호 변경 (현재 비밀번호 확인 필요)
               → site_config 테이블에 해시 저장
```

---

## 환경 변수 (.env.local — git 제외됨)

| 변수명 | 역할 | 비고 |
|--------|------|------|
| `NEXT_PUBLIC_SUPABASE_URL` | Supabase 프로젝트 URL | 필수 |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | Supabase anon 키 | 필수 |
| `SUPABASE_SERVICE_ROLE_KEY` | 서버 전용 서비스 롤 키 | 필수, 절대 노출 금지 |
| `NEXT_PUBLIC_SITE_URL` | 사이트 URL | 필수 |
| `VIEW_PASSWORD` | 초기 열람 비밀번호 (숫자 4자리) | 기본값: `1234` |
| `ADMIN_PASSWORD` | 초기 관리자 비밀번호 | 기본값: `12341234` |
| `SESSION_SECRET` | 세션 서명 HMAC 시크릿 (64자 hex) | 절대 노출 금지 |
| `UPSTASH_REDIS_REST_URL` | Upstash Redis URL | Rate Limiting용 |
| `UPSTASH_REDIS_REST_TOKEN` | Upstash Redis 토큰 | Rate Limiting용 |
| `MICROLINK_API_KEY` | URL 제목 자동 추출 | 선택 |

---

## 핵심 파일 목록

### 신규 생성
| 파일 | 역할 |
|------|------|
| `src/lib/auth/session.ts` | Web Crypto API 세션 토큰 생성·검증·비밀번호 해시 |
| `src/actions/site-auth.ts` | 로그인·열람PW변경·관리자PW변경 서버 액션 |
| `src/app/admin/page.tsx` | 관리자 설정 페이지 (비밀번호 변경 2종) |
| `supabase/migrations/009_site_config.sql` | `site_config` 테이블 생성 (비밀번호 해시 저장) |

### 주요 수정
| 파일 | 변경 내용 |
|------|-----------|
| `src/middleware.ts` | Supabase Auth → `klh_session` 쿠키 검증으로 교체 |
| `src/app/(auth)/login/page.tsx` | 4자리 숫자 전용 입력으로 변경 |
| `src/components/layout/TeamHeader.tsx` | userId/role 제거, 설정→`/admin` 링크, `signOut` 교체 |
| `src/actions/items.ts` | 모든 `createClient()` → `createServiceClient()`, auth 가드 제거 |
| `src/app/(dashboard)/dashboard/page.tsx` | auth 체크 제거 |
| `src/app/explore/page.tsx` | auth 체크 제거, service client 사용 |
| `src/app/api/og/route.ts` | Supabase auth → 세션 쿠키 검증 |
| `src/app/share/page.tsx` + `ShareContent.tsx` | 서버 컴포넌트에서 팀 목록 조회 후 클라이언트에 전달 |

### 삭제된 파일
- `src/actions/auth.ts` (구 PIN 인증 서버 액션)
- `src/lib/security/pin-blocklist.ts`
- `src/app/(auth)/signup/page.tsx`
- `src/app/(dashboard)/settings/` (4개 파일)
- `src/app/group-settings/` (2개 파일)
- `src/app/auth/callback/route.ts`
- `src/app/join/page.tsx`
- `src/app/(dashboard)/onboarding/page.tsx`
- `src/app/api/auth/naver/` (2개 파일)

---

## Supabase 마이그레이션 상태

| 파일 | 상태 |
|------|------|
| 001~008 | ✅ 이전 세션에서 실행 완료 |
| **009_site_config.sql** | ⚠️ **아직 실행 안 함 — 반드시 실행 필요** |

### 009 실행 방법
1. Supabase Dashboard 접속
2. SQL Editor 열기
3. `supabase/migrations/009_site_config.sql` 내용 복사·붙여넣기 후 실행

---

## 보안 점검 결과 (2026-04-28)

- `.env.local` — `.gitignore` 제외 ✅
- `client_secret_*.json` — `.gitignore` 제외 ✅
- 소스 코드 내 하드코딩 시크릿 없음 (`process.env.*`로만 참조) ✅
- `SUPABASE_SERVICE_ROLE_KEY` 서버 전용 파일에서만 사용 ✅

---

## 다음 작업 시 할 일

1. **Supabase에서 009 마이그레이션 실행** (미완료 — 반드시 필요)
2. **배포 전 비밀번호 변경**: `/admin`에서 열람·관리자 비밀번호 변경
3. **배포 환경 `SESSION_SECRET` 재생성**: `openssl rand -hex 32`
4. Vercel 배포 시 환경 변수 전체 입력 확인

---

## 개발 서버 실행

```bash
cd "D:\AI_Dev_Work\2026y\26y_04m_12d_knowledge-link-hub"
npm run dev
# 기본 포트: 3000 또는 --port 옵션으로 지정
```

---

## 기술 참고

### 세션 토큰 구조
```
{expiry_unix_ms}.{hmac_sha256_hex}
```
- `SESSION_SECRET`으로 HMAC-SHA256 서명
- `klh_session` 쿠키 (HttpOnly, SameSite=Strict, Secure on prod)
- 유효기간 7일

### 비밀번호 저장 구조 (`site_config` 테이블)
| key | value |
|-----|-------|
| `view_password_hash` | 열람 비밀번호의 HMAC-SHA256 hex |
| `admin_password_hash` | 관리자 비밀번호의 HMAC-SHA256 hex |

- UI를 통해 변경하기 전까지: `ADMIN_PASSWORD` 환경 변수로 fallback (평문 비교)
- 변경 후: 해시값이 테이블에 저장되고 이후부터 해시 비교

---

## 이어하기용 명령어

```bash
cd "D:\AI_Dev_Work\2026y\26y_04m_12d_knowledge-link-hub"
git log --oneline -5
git status
npm run build
npm run dev
```

---

## 이어하기용 지시문

```
D:\AI_Dev_Work\2026y\26y_04m_12d_knowledge-link-hub 프로젝트 이어서 작업.
.resume.md 참고 — 단일 비밀번호 인증으로 교체 완료.
009_site_config.sql 마이그레이션 아직 Supabase에 적용 안 됨 → 최우선 확인.
```
