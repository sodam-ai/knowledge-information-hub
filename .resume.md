# Knowledge Information Hub — 작업 재개용 기록

> 마지막 업데이트: 2026-04-30  
> 최종 커밋: (이번 세션 커밋 예정)  
> 브랜치: master  
> 저장소: https://github.com/sodam-ai/knowledge-link-hub  
> **전체 상태: 프로덕션 배포 가능 ✅**

---

## 📁 프로젝트 경로

```
D:\AI_Dev_Work\2026y\26y_04m_12d_knowledge-link-hub\
```

## 📂 주요 파일 위치

```
knowledge-information-hub/
├── src/
│   ├── actions/
│   │   ├── site-auth.ts          # 로그인·비밀번호 변경 서버 액션
│   │   ├── items.ts              # 콘텐츠 CRUD + 검색(pg_trgm + 태그) 서버 액션
│   │   ├── teams.ts              # 그룹 서버 액션
│   │   └── collections.ts        # 컬렉션 서버 액션
│   ├── app/
│   │   ├── (auth)/login/         # 열람 비밀번호 입력 페이지
│   │   ├── (dashboard)/
│   │   │   └── dashboard/page.tsx  # 메인 피드 페이지 (팀 자동 생성)
│   │   ├── admin/                # 관리자 설정
│   │   ├── api/og/               # OG 메타 추출 API (SSRF 방어 포함)
│   │   ├── onboarding/page.tsx   # /dashboard redirect (페이지 제거됨)
│   │   └── settings/             # 앱 설정
│   ├── components/
│   │   ├── items/
│   │   │   ├── ItemCard.tsx      # 항목 카드 (모바일 최적화)
│   │   │   ├── ItemFeed.tsx      # 피드 + 날짜/카테고리/타입 필터 + 캘린더 포탈
│   │   │   ├── SaveItemButton.tsx  # 링크/노트 저장 바텀시트 (파일 탭 제거됨)
│   │   │   └── SearchBar.tsx     # 자연어+#태그 검색
│   │   └── layout/
│   │       └── TeamHeader.tsx    # 헤더 (ShieldCheck 관리자 아이콘)
│   └── middleware.ts             # 세션 쿠키 검증
├── supabase/migrations/          # SQL 001~011
│   ├── 010_collections.sql       # 컬렉션 테이블 + FK + RLS
│   └── 011_item_category.sql     # items.category 컬럼 + 인덱스
├── public/
│   ├── favicon.ico               # 앱 파비콘
│   ├── icon-192.png              # PWA 아이콘
│   └── icon-512.png              # PWA 아이콘
├── .env.local                    # 실제 환경 변수 (git 미추적)
├── LICENSE                       # MIT (SoDam AI Studio)
└── README.md
```

---

## ✅ 완료된 작업 (이번 세션: 2026-04-30)

### 이전 세션까지 완료 (커밋 2개 ahead of origin)
- 앱 이름 → "Knowledge Information Hub" 전체 변경
- /onboarding 제거 — 로그인 후 대시보드 직접 진입
- dashboard/page.tsx: 팀 없을 때 자동 생성
- favicon.ico + PWA 아이콘 생성

### 이번 세션 추가 개선 (미커밋)
| 번호 | 항목 | 상태 |
|------|------|------|
| 1 | 검색 개선 (자연어 + #태그 + pg_trgm) | ✅ |
| 2 | 팝업 짤림 현상 — createPortal 캘린더 | ✅ |
| 3 | URL 제목 자동 추출 오류 개선 | ✅ |
| 4 | 새 항목 저장 오류 (title_failed 버그) | ✅ |
| 5 | 모바일 최적화 (dvh, 터치 타겟) | ✅ |
| 6 | 파일 업로드 탭 제거 | ✅ |
| 7 | 관리자 아이콘 ShieldCheck 변경 | ✅ |
| 8 | 날짜 필터 세밀화 (어제/3일/90일 추가) | ✅ |
| 9 | 카테고리 검색 (12개 전체 표시 + 배지) | ✅ |

### DB 마이그레이션 (원격 Supabase 적용 완료)
- `supabase db push` 실행 → 010, 011 원격 적용 완료 ✅
- PGRST204 오류 원인: `category` 컬럼 미적용 → 해결됨

---

## 📋 남아 있는 작업

| 우선순위 | 작업 | 비고 |
|----------|------|------|
| 즉시 | git commit + push | 미커밋 변경사항 전체 |
| 권장 | Vercel 재배포 | 환경변수 설정 후 |
| 선택 | GitHub 저장소 이름 변경 | knowledge-information-hub |
| 선택 | Supabase pg_cron 설정 | 소프트 삭제 30일 자동 정리 |

---

## 🏁 빌드 상태

```
[✅] npm run build 통과 — 9개 라우트, 오류 0 (2026-04-30)
[✅] supabase db push 완료 — 010, 011 원격 적용
[✅] PGRST204 오류 해결 확인 (REST API 직접 INSERT 테스트 통과)
[🔲] git commit + push
[🔲] Vercel 배포
```

---

## 🌐 환경 변수 (.env.local — git 제외됨)

| 변수명 | 역할 |
|--------|------|
| `NEXT_PUBLIC_SUPABASE_URL` | Supabase 프로젝트 URL |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | Supabase anon 키 |
| `SUPABASE_SERVICE_ROLE_KEY` | 서버 전용 서비스 롤 키 (절대 노출 금지) |
| `NEXT_PUBLIC_SITE_URL` | 사이트 URL |
| `VIEW_PASSWORD` | 열람 비밀번호 |
| `ADMIN_PASSWORD` | 관리자 비밀번호 |
| `SESSION_SECRET` | 세션 서명 HMAC 시크릿 |
| `UPSTASH_REDIS_REST_URL` | Upstash Redis (Rate Limiting) |
| `UPSTASH_REDIS_REST_TOKEN` | Upstash Redis 토큰 |

---

## 🔐 보안 주의사항

- `.env.local` — `.gitignore` 제외 ✅ (절대 커밋 금지)
- `SUPABASE_SERVICE_ROLE_KEY` — 서버 전용 파일에서만 사용 ✅
- `git add` 시 반드시 명시적 파일명만 사용 (`git add .` 금지)
