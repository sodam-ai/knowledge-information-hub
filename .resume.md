# Knowledge Link Hub — 작업 재개용 기록

> 마지막 업데이트: 2026-04-29  
> 최종 커밋: 7df1a97 (UX 개선 — 스크롤 리셋·모바일 패딩·목록 갱신)  
> 브랜치: master  
> 저장소: https://github.com/sodam-ai/knowledge-link-hub  
> **전체 상태: 프로젝트 완성 ✅ — 프로덕션 배포 가능**

---

## 📁 프로젝트 경로

```
D:\AI_Dev_Work\2026y\26y_04m_12d_knowledge-link-hub\
```

## 📂 관련 폴더 / 파일 위치

```
knowledge-link-hub/
├── src/
│   ├── actions/
│   │   ├── site-auth.ts          # 로그인·비밀번호 변경 서버 액션
│   │   ├── items.ts              # 콘텐츠 CRUD 서버 액션
│   │   └── teams.ts              # 그룹 서버 액션 + joinTeamByCodeNoAuth()
│   ├── app/
│   │   ├── (auth)/login/         # 열람 비밀번호 입력 페이지
│   │   ├── (dashboard)/
│   │   │   ├── dashboard/page.tsx  # 메인 피드 페이지
│   │   │   ├── error.tsx           # 에러 바운더리 [신규]
│   │   │   └── loading.tsx         # 스켈레톤 로딩 [신규]
│   │   ├── not-found.tsx         # 전역 404 [신규]
│   │   ├── admin/                # 관리자 설정
│   │   ├── api/og/               # OG 메타 추출 API
│   │   ├── explore/              # 공개 그룹 탐색
│   │   ├── join/page.tsx         # 초대 코드 참여 UI [신규]
│   │   ├── onboarding/page.tsx   # 새 그룹 만들기
│   │   └── share/                # 링크 공유
│   ├── components/
│   │   ├── items/
│   │   │   ├── ItemCard.tsx      # 모바일 375px + aria-label [수정]
│   │   │   ├── ItemFeed.tsx      # 날짜 그룹 헤더·핀 섹션
│   │   │   ├── SaveItemButton.tsx  # 저장 바닥 시트
│   │   │   └── SearchBar.tsx
│   │   ├── layout/
│   │   │   └── TeamHeader.tsx    # 모바일 수정 + aria-label [수정]
│   │   └── ui/                   # Toast 시스템
│   ├── lib/
│   │   ├── auth/session.ts       # Web Crypto API 세션
│   │   ├── supabase/             # Supabase 클라이언트
│   │   ├── security/             # Rate Limiter
│   │   └── validations/          # Zod 스키마
│   └── middleware.ts             # 세션 쿠키 검증
├── supabase/migrations/          # SQL 001~009
├── .env.example                  # 환경 변수 예시 (실제 값 없음)
├── .env.local                    # 실제 환경 변수 (git 미추적)
├── .gitignore
├── LICENSE                       # MIT (SoDam AI Studio)
├── README.md                     # 한국어 문서 [최신화됨]
├── README.en.md                  # 영어 문서 [신규]
└── .resume.md                    # 이 파일
```

## 📍 현재 작업 위치

```
브랜치: master
작업: Phase 4 완료 직후 — .resume.md 갱신 중
다음: Phase 5 — docs 커밋 + push
```

---

## ✅ 완료된 작업

### 세션 1 (커밋 40c970c)
- OG 썸네일 E2E 수정 (클라이언트 사이드 OG 수집 → 서버 액션 전달)
- ItemFeed stale 상태 수정 (key 재마운트 방식)
- 온보딩 페이지 신규 구현 (`/onboarding`)
- 날짜별 피드 그룹 헤더, 핀 섹션 구현
- GitHub push 완료

### 세션 2 (커밋 54c2af1)
- `src/app/(dashboard)/error.tsx` — 에러 바운더리 신규 생성
- `src/app/(dashboard)/loading.tsx` — 스켈레톤 로딩 신규 생성
- `src/app/not-found.tsx` — 전역 404 페이지 신규 생성
- `src/app/join/page.tsx` — 초대 코드 참여 UI 신규 생성
- `src/actions/teams.ts` — `joinTeamByCodeNoAuth()` 추가
- `src/components/items/ItemCard.tsx` — 모바일 375px max-w 수정 + aria-label
- `src/components/layout/TeamHeader.tsx` — 모바일 팀명 말줄임 수정 + aria-label
- `npm run build` 통과 (12개 라우트, 오류 0)
- GitHub push 완료

### 세션 2 계속 (docs 커밋 예정)
- 보안 점검 완료 (git 추적 파일 내 민감정보 없음 확인)
- `README.md` 최신 기능 반영 (/join, 모바일, error.tsx, aria-label, 폴더 구조)
- `README.en.md` 신규 생성 (한국어 README와 동일 구조)
- `.resume.md` 상세 포맷으로 갱신 (현재 작업)

---

## 🔄 진행 중인 작업

없음 — 모든 기능 구현 완료. 현재 문서 정리 중.

---

## 📋 남아 있는 작업

| 우선순위 | 작업 | 비고 |
|----------|------|------|
| 즉시 | Phase 5: docs 커밋 + push | README.md, README.en.md, .resume.md |
| 선택 | Supabase pg_cron 설정 | 소프트 삭제 30일 자동 정리 |
| 선택 | Vercel 환경변수 변경 | VIEW_PASSWORD, ADMIN_PASSWORD, SESSION_SECRET, Upstash Redis |
| ✅ 완료 | SaveItemButton 모바일 패딩 | px-4 pt-3 pb-4 sm:p-5 적용 |
| ✅ 완료 | ItemFeed 스크롤 리셋 수정 | key prop에서 totalCount 제거 |
| ✅ 완료 | ItemFeed 목록 즉시 갱신 | totalCount 변화 감지 useEffect 추가 |
| ✅ 완료 | 썸네일 Phase 1.5 검증 | og_image → thumbnail_url 저장 + 카드 표시 확인 |

---

## 🚀 다음 최우선 작업

```bash
cd "D:\AI_Dev_Work\2026y\26y_04m_12d_knowledge-link-hub"
git add README.md README.en.md .resume.md
git status  # 확인 후
git commit -m "docs: README.en.md 신규 + README.md 최신 기능 반영 + .resume.md 갱신"
git push origin master
```

---

## 📝 변경된 파일 (커밋별)

### 커밋 54c2af1
| 파일 | 변경 |
|------|------|
| `src/actions/teams.ts` | `joinTeamByCodeNoAuth()` 함수 추가 |
| `src/app/(dashboard)/error.tsx` | 신규 생성 |
| `src/app/(dashboard)/loading.tsx` | 신규 생성 |
| `src/app/join/page.tsx` | 신규 생성 |
| `src/app/not-found.tsx` | 신규 생성 |
| `src/components/items/ItemCard.tsx` | max-w 반응형 3곳 수정 + aria-label 3개 추가 |
| `src/components/layout/TeamHeader.tsx` | max-w 반응형 1곳 수정 + aria-label 3개 추가 |

### docs 커밋 (예정)
| 파일 | 변경 |
|------|------|
| `README.md` | /join, 모바일, error/loading/not-found, 폴더 구조, 비개발자 가이드 업데이트 |
| `README.en.md` | 신규 생성 (전체 영어 문서) |
| `.resume.md` | 상세 포맷으로 전체 갱신 |

---

## 🔧 핵심 수정 내용

### joinTeamByCodeNoAuth() — 인증 없이 초대 코드로 그룹 참여
- 기존 `joinTeam()`은 Supabase user auth 필요 → 이 프로젝트에선 사용 불가
- `createServiceClient()` 사용 → user_id 없이 team lookup 가능
- invite_code 대소문자 정규화(toUpperCase), 만료일 검증 포함
- user_teams 테이블 삽입 없음 (사용자 개념 없는 구조)

### Next.js 규약 파일 3종
- `error.tsx`: "use client" 필수, `reset()` 함수 수신 — 다시 시도 버튼에 연결
- `loading.tsx`: Suspense fallback — animate-pulse 스켈레톤, 외부 라이브러리 없음
- `not-found.tsx`: 서버 컴포넌트 — notFound() 호출 시 자동 렌더

### 모바일 375px 반응형
- `max-w-[140px]` → `max-w-[100px] sm:max-w-[180px]` (TeamHeader 팀명)
- `max-w-[80px]` → `max-w-[60px] sm:max-w-[80px]` (ItemCard hostname)
- `max-w-[150px]` → `max-w-[100px] sm:max-w-[150px]` (ItemCard 링크 설명)

---

## ⚠️ 발생한 문제 / 원인 추정 / 해결 여부

| 문제 | 원인 | 해결 여부 |
|------|------|-----------|
| git add 괄호 경로 오류 | Windows bash에서 `\(` 이스케이프 미작동 | ✅ 해결 — 큰따옴표 경로로 대체 |
| Fact-Forcing Gate 반복 차단 | ECC 플러그인 pre:edit-write hook | ✅ 해결 — 매 Edit 전 facts 텍스트 선제 제시 |
| joinTeam() 인증 오류 | Supabase user auth 필요한 함수 사용 | ✅ 해결 — joinTeamByCodeNoAuth() 신규 작성 |
| 병렬 Edit 충돌 | 동일 파일에 두 Edit 동시 전송 | ✅ 해결 — 순차 Edit으로 변경 |

---

## 🐛 남아 있는 이슈

| 우선순위 | 이슈 | 영향 |
|----------|------|------|
| 낮음 | Microlink 서버사이드 도달 불가 (Windows Node.js) | 클라이언트 OG로 우회 완료, 실제 영향 없음 |
| 낮음 | ItemFeed key 재마운트 시 스크롤 위치 리셋 | UX 개선 여지 있음, 기능에 영향 없음 |
| 낮음 | SaveItemButton 375px 패딩 과밀집 | 시각적 개선 여지, 기능에 영향 없음 |
| 낮음 | Supabase pg_cron 미설정 | 소프트 삭제 항목 자동 정리 안 됨 |

---

## 🏁 체크포인트

```
[✅] 기능 구현 완료 — 커밋 54c2af1
[✅] npm run build 통과 — 12개 라우트, 오류 0
[✅] GitHub push 완료 — master 브랜치
[✅] 보안 점검 — git 추적 파일 내 민감정보 없음
[✅] README.md 최신화
[✅] README.en.md 신규 생성
[✅] .resume.md 갱신
[🔲] docs 커밋 + push (다음 작업)
[🔲] Vercel 프로덕션 배포 (선택)
```

---

## ▶️ 이어하기용 명령어

### 즉시 실행 — docs 커밋 + push

```bash
cd "D:\AI_Dev_Work\2026y\26y_04m_12d_knowledge-link-hub"
git add README.md README.en.md .resume.md
git status
git commit -m "docs: README.en.md 신규 + README.md 최신 기능 반영 + .resume.md 갱신"
git push origin master
git log --oneline -3
```

### 상태 확인

```bash
cd "D:\AI_Dev_Work\2026y\26y_04m_12d_knowledge-link-hub"
git log --oneline -5
git status
npm run dev -- --port 8002
# 브라우저: http://localhost:8002
```

### 빌드 검증

```bash
cd "D:\AI_Dev_Work\2026y\26y_04m_12d_knowledge-link-hub"
npm run build
```

---

## 🔐 보안 주의사항

- `.env.local` — `.gitignore` 제외 ✅ (절대 커밋 금지)
- `SUPABASE_SERVICE_ROLE_KEY` — 서버 전용 파일에서만 사용 ✅
- `client_secret_*.json` — `.gitignore` 패턴 제외 ✅
- 소스 코드 내 하드코딩 시크릿 없음 ✅
- `git add` 시 반드시 명시적 파일명만 사용 (git add . 금지)

---

## 🗄️ Supabase 마이그레이션 상태

| 파일 | 상태 |
|------|------|
| 001~008 | ✅ 실행 완료 |
| **009_site_config.sql** | ⚠️ **Supabase에 아직 미적용 시 반드시 실행** |

009번 실행 전까지 `/admin` 비밀번호 변경 기능 미작동.

---

## 🌐 환경 변수 (.env.local — git 제외됨)

| 변수명 | 역할 | 비고 |
|--------|------|------|
| `NEXT_PUBLIC_SUPABASE_URL` | Supabase 프로젝트 URL | 필수 |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | Supabase anon 키 | 필수 |
| `SUPABASE_SERVICE_ROLE_KEY` | 서버 전용 서비스 롤 키 | 필수, 절대 노출 금지 |
| `NEXT_PUBLIC_SITE_URL` | 사이트 URL | 필수 |
| `VIEW_PASSWORD` | 초기 열람 비밀번호 (숫자 4자리) | 배포 전 변경 |
| `ADMIN_PASSWORD` | 초기 관리자 비밀번호 | 배포 전 변경 |
| `SESSION_SECRET` | 세션 서명 HMAC 시크릿 (64자 hex) | 절대 노출 금지 |
| `UPSTASH_REDIS_REST_URL` | Upstash Redis URL | Rate Limiting용 |
| `UPSTASH_REDIS_REST_TOKEN` | Upstash Redis 토큰 | Rate Limiting용 |
| `MICROLINK_API_KEY` | URL 제목 자동 추출 | 선택, Windows 미도달 이슈 |
