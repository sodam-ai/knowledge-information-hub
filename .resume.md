# Knowledge Link Hub — 이어하기 기록

> 마지막 업데이트: 2026-04-11
> 작성 목적: 작업 중단 시 즉시 이어서 진행할 수 있는 체크포인트

---

## 프로젝트 경로

```
D:\AI_Dev_Work\2026y\26y_04m_12d_knowledge-link-hub
```

## 관련 폴더 / 파일 위치

| 역할 | 경로 |
|------|------|
| 소스코드 | `src/` |
| 서버 액션 | `src/actions/` (auth.ts, items.ts, teams.ts) |
| 페이지 | `src/app/` |
| 컴포넌트 | `src/components/` (items/, layout/, ui/) |
| DB 마이그레이션 | `supabase/migrations/` (001~008) |
| 환경 변수 | `.env.local` (gitignore됨) |
| 환경 변수 예시 | `.env.example` |
| 타입 정의 | `src/types/index.ts` |
| Zod 검증 | `src/lib/validations/index.ts` |
| 보안 | `src/lib/security/` (rate-limit.ts, pin-blocklist.ts) |
| OG 메타 추출 API | `src/app/api/og/route.ts` |
| 토스트 시스템 | `src/components/ui/toast.tsx` |

## 현재 작업 위치

- **브랜치**: master
- **GitHub**: sodam-ai/knowledge-link-hub (비공개)
- **Phase**: Phase 1 MVP + 그룹 플랫폼 + UI 고도화 + 검색 강화 완료
- **개발 서버**: `npm run dev -- --port 8001` (포트 8001)
- **Supabase**: sctozumzobwztraxxpva.supabase.co

---

## 완료된 작업

### Phase 1 MVP
- [x] 아이디 + 4자리 PIN 간편 인증 (HMAC-SHA256, 취약 PIN 차단)
- [x] Google/GitHub/Kakao OAuth
- [x] 그룹 생성/초대/참여 (초대 코드 72시간 유효)
- [x] 링크·노트 저장, 태그, 검색, 소프트 삭제
- [x] RLS + SECURITY DEFINER 보안 패턴
- [x] 마이그레이션 001~007 적용

### 그룹 플랫폼 업그레이드
- [x] 공개/비공개 그룹 + 카테고리 분류
- [x] 공개 500명 / 비공개 20명 인원 제한 + 사용자 10그룹 한도
- [x] /explore 공개 그룹 탐색 + /group-settings 멤버 관리

### UI 고도화 (Phase 2 — 2026-04-10~11)
- [x] 토스트 알림 시스템 (React Context + createPortal + RAF 애니메이션)
- [x] URL OG 메타 자동 추출 (`/api/og` — SSRF 방어, 인증 필수, 512KB 제한)
- [x] SaveItemButton: 바닥 시트 패턴 (모바일 슬라이드, 데스크톱 모달)
- [x] 클립보드 자동 감지 + OG 미리보기 (600ms 디바운스)
- [x] ItemCard: 컴팩트 2행 레이아웃, flex-wrap 제거
- [x] ItemCard: DetailSheet 상세 시트 (바닥 시트, 전체 내용·썸네일·태그·수정·삭제)
- [x] ItemCard: 링크 제목 클릭 → 외부 URL 바로 연결 (e.stopPropagation)
- [x] ItemCard: 카드 클릭 → 상세 시트 열기
- [x] ItemFeed: 태그 필터링 (tagFilter 토글)
- [x] IntersectionObserver 무한 스크롤 (isLoadingRef + itemsLengthRef)
- [x] 핀 고정 + 인라인 수정

### 검색 강화 (Phase 3 — 2026-04-11)
- [x] 검색 최소 길이 2자 → 1자로 변경
- [x] URL 필드 검색 추가 (title + content + url)
- [x] 1자 쿼리: ilike 직접 매칭 (pg_trgm 우회)
- [x] 2자 이상: pg_trgm similarity RPC (url 컬럼 추가)
- [x] migration 008_search_rpc_url.sql 생성 + Supabase 적용 완료
- [x] SearchBar 플레이스홀더 업데이트 ("제목, 내용, URL")

### 보안 (전체 Phase 공통)
- [x] Rate Limiting: Upstash Redis (로그인 5/10분, 가입 3/시간, OG 20/분)
- [x] SSRF 방어: 10개 regex 패턴 (사설 IP, 루프백, 클라우드 메타데이터)
- [x] 보안 헤더: next.config.ts (CSP, HSTS, X-Frame-Options)
- [x] PIN 취약 번호 차단 + HMAC-SHA256 해싱
- [x] Zod 입력 검증 + HTML 이스케이프

### 문서 + 인프라 (2026-04-11)
- [x] README.md 전면 갱신 (한국어 + 영어, 비개발자 가이드 포함)
- [x] LICENSE: MIT, SoDam AI Studio
- [x] .resume.md 업데이트
- [x] TypeScript 오류 0건, 빌드 성공 (exit code 0)

---

## 진행 중인 작업

- 없음 (현재 세션 작업 모두 완료)

---

## 남아 있는 작업

### 즉시 가능
- [ ] DB 중복 테스트 그룹 정리
- [ ] Vercel 배포 (환경 변수 설정 + 도메인 연결)

### Phase 1.5
- [ ] 파일 업로드 (Supabase Storage)
- [ ] 헤더 멤버 수 표시

### Phase 2
- [ ] AI 검색 (벡터 임베딩)
- [ ] 댓글 기능
- [ ] 소프트 삭제 30일 자동 정리 (pg_cron)

---

## 다음 최우선 작업

1. **사용자 테스트 피드백 반영** — 사용자가 테스트 후 추가 개선사항 보고 대기
2. **Vercel 배포** — 환경 변수 설정 + Supabase 리다이렉트 URL 등록
3. **파일 업로드** — Supabase Storage 버킷 생성 + 아이템 첨부 기능

---

## 변경된 파일 (이번 세션 — 2026-04-10~11)

| 파일 | 변경 내용 |
|------|-----------|
| `src/components/ui/toast.tsx` | 신규: 토스트 알림 시스템 (Provider + Portal + RAF) |
| `src/app/layout.tsx` | ToastProvider 래핑 추가 |
| `src/app/api/og/route.ts` | 신규: OG 메타 추출 API (SSRF 방어, 인증, Rate Limit) |
| `src/components/items/SaveItemButton.tsx` | 전면 재작성: 바닥 시트 + 클립보드 + OG 미리보기 |
| `src/components/items/ItemCard.tsx` | 전면 재작성: DetailSheet + 링크 직접 연결 + 컴팩트 레이아웃 |
| `src/components/items/ItemFeed.tsx` | 태그 필터 + IntersectionObserver 무한 스크롤 |
| `src/components/items/SearchBar.tsx` | 최소 1자 검색 + URL 검색 + 플레이스홀더 개선 |
| `src/actions/items.ts` | searchItems: 1자 ilike fallback + url 필드 추가 |
| `src/lib/security/rate-limit.ts` | Upstash Redis 실사용 연결 |
| `src/app/(dashboard)/dashboard/page.tsx` | SearchBar teamId prop, 전체 피드 뷰 |
| `src/app/(auth)/login/page.tsx` | PIN 자동 제출, 오류 시 리셋 |
| `src/app/(auth)/signup/page.tsx` | PIN 진행 도트, 동일 스타일링 |
| `src/app/(dashboard)/settings/ProfileSettings.tsx` | 이름 수정 기능 |
| `src/app/(dashboard)/settings/page.tsx` | PIN 변경 섹션 추가 |
| `src/app/(dashboard)/settings/PinSettings.tsx` | 신규: PIN 변경 컴포넌트 |
| `src/actions/auth.ts` | changePin 액션 + Rate Limit 통합 |
| `supabase/migrations/008_search_rpc_url.sql` | 신규: search_items RPC에 url 검색 추가 |
| `README.md` | 전면 갱신 (한국어 + 영어 + 비개발자 가이드) |
| `.resume.md` | 전면 갱신 (현재 체크포인트) |

---

## 핵심 수정 내용

### 검색 강화
- `searchItems` 최소 길이 2→1, url 필드를 ilike 쿼리에 추가
- pg_trgm은 2자 이상 필요 → 1자 쿼리는 ilike 직접 매칭으로 fallback
- migration 008: RPC에 `i.url ILIKE` 조건 + `similarity(i.url, p_query)` 추가

### 카드 상호작용 분리
- `<a>` 태그: `onClick={e => e.stopPropagation()}` → 링크 바로 연결
- `article`: `onClick={handleCardClick}` → 상세 시트 열기
- 액션 버튼/태그/수정 폼: 각각 `stopPropagation()` 적용

### 바닥 시트 패턴
- 2-div 구조로 CSS transform 충돌 방지 (포지셔닝 vs 애니메이션 분리)
- `requestAnimationFrame → setState(true)` 패턴으로 CSS transition 트리거

---

## 발생한 문제 / 원인 추정 / 해결 여부

| 문제 | 원인 | 해결 |
|------|------|------|
| `__webpack_modules__[moduleId] is not a function` | stale `.next` 캐시 | `.next` 폴더 삭제 후 재시작 ✅ |
| 포트 8001 EADDRINUSE | 이전 세션 서버 미종료 | `npx kill-port 8001` 후 재시작 ✅ |
| sm:translate-y-0 vs 센터링 transform 충돌 | 같은 요소에 두 transform 적용 | 2-div 패턴 (외부=포지셔닝, 내부=애니메이션) ✅ |
| 무한 스크롤 stale closure | useCallback deps에 items.length 직접 사용 | isLoadingRef + itemsLengthRef로 해결 ✅ |
| 1자 검색 시 pg_trgm 결과 없음 | trigram은 3글자 토큰 → 1자에서 recall 0 | 1자 쿼리 → ilike 직접 매칭 fallback ✅ |

---

## 남아 있는 이슈

- Kakao/Naver OAuth: 개발자 계정 등록 전까지 실제 로그인 불가 (UI만 구현됨)
- 소프트 삭제 후 30일 자동 정리: Supabase pg_cron 설정 필요
- DB 테스트 데이터 정리 필요

---

## 체크포인트

```
- Git 저장소: sodam-ai/knowledge-link-hub (비공개)
- 브랜치: master
- 빌드 상태: TypeScript 오류 0건, 프로덕션 빌드 성공 ✅
- 마이그레이션: 001~008 모두 Supabase 적용 완료
- 개발 서버: localhost:8001
- Supabase: sctozumzobwztraxxpva.supabase.co
```

---

## 이어하기용 명령어

```bash
# 프로젝트 폴더로 이동
cd "D:\AI_Dev_Work\2026y\26y_04m_12d_knowledge-link-hub"

# 현재 상태 확인
git log --oneline -10
git status

# 개발 서버 실행
npm run dev -- --port 8001

# 타입 체크
npx tsc --noEmit

# 빌드 테스트
npm run build
```

---

## 이어하기용 지시문

```
D:\AI_Dev_Work\2026y\26y_04m_12d_knowledge-link-hub 프로젝트 이어서 작업.
.resume.md 파일 기준으로 현재 상태 점검 후 "다음 최우선 작업"부터 진행.
브랜치: master / 포트: 8001 / Supabase: sctozumzobwztraxxpva.supabase.co
```
